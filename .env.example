XMTP_WALLET_KEY= # the private key of the wallet (with or without 0x prefix)
XMTP_DB_ENCRYPTION_KEY= # a 32-byte hex encryption key for local database
XMTP_ENV=dev # the environment to use: local, dev, or production
XMTP_DB_DIRECTORY= # directory for persistent database storage (e.g. /data for Fly.io)
ANTHROPIC_API_KEY= # your Claude API key from console.anthropic.com
